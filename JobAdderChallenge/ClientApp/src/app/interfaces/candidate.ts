export interface Candidate {
  id: number,
  name: string
}

export interface CandidateDetail {
  id: number,
  name: string,
  skills: string[]
}

export interface TopCandidate {
  id: number,
  name: string,
  skills: string[]
}
